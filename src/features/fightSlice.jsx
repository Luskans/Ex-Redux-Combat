import { createSlice } from '@reduxjs/toolkit';

export const fightSlice = createSlice({
   name: 'fight',
   initialState: {
      

   },
   reducers: {
        
   }
});

export const {  } = fightSlice.actions;
export default fightSlice.reducer;